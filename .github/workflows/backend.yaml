name: Backend CI/CD

on:
  push:
    branches:
    - main
    paths:
    - 'backend/**' # Solo ejecuta si se modifica algo dentro de backend/
  pull_request:
    branches:
    - main
    paths:
    - 'backend/**' # Solo ejecuta si el PR afecta archivos dentro de backend/

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build Docker image
      run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/plotter-backend:latest ./backend

    - name: Login to DockerHub
      run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

    - name: Push Docker image to DockerHub
      run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/plotter-backend:latest
